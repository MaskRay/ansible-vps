# NB. check and update these versions before running ansible-playbook
latest:
    # apt: name=postgresql-server-dev-{{latest.postgresql}
    postgresql: 9.5
    # apt: name=clang-{{latest.clang}}
    clang: 3.7
    # apt_repository: repo='deb https://deb.nodesource.com/node_{{latest.nodejs}} {{ansible_distribution_release}} main'
    nodejs: 5.x
    # ppa:fkull/deadsnakes
    python: 3.5
    # https://influxdata.com/downloads/
    influxdb: 0.10.0-1

apt_java_installer: oracle-java8-installer
apt_java_home: /usr/lib/jvm/java-8-oracle
